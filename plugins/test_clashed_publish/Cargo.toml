[package]
name = "test_clashed_publish"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
kissa = { path = "../../kissa" }

[lib]
crate-type = ["dylib"]
